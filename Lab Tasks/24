#include<stdio.h>
int main(){
int array[100];
int i;
int n;
int sum = 0;

printf("Enter the number of n:");
scanf("%d",  &n);

printf("Enter the %d  elements:\n",n);
for (i = 0; i < n; i++){
scanf("%d", &array[i]);
sum += array[i];
}
printf("The sum of array elements: %d\n", sum);
return 0;
}
